server:
  port: 9004

spring:
  application:
    name: cloudalibaba-provider-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848


management:
  endpoints:
    web:
      exposure:
        include: '*'